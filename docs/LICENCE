# LICENCE — docs/

This folder contains the documentation layer for `iontrap-dynamics`.

## Licence split inside `docs/`

- Coastline material in `docs/` root is licensed under **CC BY-SA 4.0**
  (`CC-BY-SA-4.0`).
- Sail material under `docs/tutorials/` is licensed under
  **CC BY-NC-SA 4.0** (`CC-BY-NC-SA-4.0`).

`docs/tutorials/` currently holds a single placeholder landing page
(`tutorials/index.md`) pending Phase 1.E content; the placeholder is
Sail material and covered by CC BY-NC-SA 4.0 per the split above.

## Scope

Covered by CC BY-SA 4.0 in this folder today:

- `index.md` — welcome page
- `getting-started.md` — installation and quick start
- `framework.md` — high-level design rules
- `conventions.md` — live render of the root `CONVENTIONS.md` via
  `pymdownx.snippets` include (the text originates at the repo
  root and is licensed there; this page is a rendered view under
  the same CC BY-SA 4.0 terms)
- `phase-1-architecture.md` — concrete public-API reference
- `boundary-decision-tree.md` — contributor scope decision tree
- Any future Coastline-level `.md` files added to `docs/` root

Also in `docs/` but governed by a different licence:

- `stylesheets/tokens.css` — vendored from `threehouse-plus-ec/cd-rules`,
  remains **MIT** per `../assets/SOURCE.md` (this is a mirror of
  `assets/tokens.css` maintained by `tools/fetch_assets.sh`).
- `stylesheets/extra.css` — site-local styling authored here, CC BY-SA 4.0
  unless a file-level SPDX identifier states otherwise.

## Upstream assets and fonts

- If files from `assets/` are referenced here, they retain the MIT licence and
  provenance declared in `../assets/LICENCE` and `../assets/SOURCE.md`.
- External fonts referenced by the docs site remain under their upstream SIL
  OFL 1.1 terms and are not redistributed here unless explicitly vendored.

## Precedence

If an individual file inside `docs/` declares a different SPDX identifier, that
file-level declaration takes precedence over this folder-level default.

## Endorsement Marker

Local candidate framework under active stewardship. No external endorsement is
implied.
