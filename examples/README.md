# Examples

This directory is reserved for runnable, install-light examples that
exercise the public API from a fresh checkout. The current worked
examples live in `docs/tutorials/` and the committed demo scripts under
`tools/run_demo_*.py`; promote examples here when they are intended to
be copied or run independently of the documentation build.
